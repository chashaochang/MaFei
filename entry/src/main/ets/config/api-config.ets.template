/**
 * API配置文件模板
 * 
 * 使用说明：
 * 1. 复制此文件并重命名为 api-config.ets
 * 2. 填入你的弹弹play API密钥信息
 * 3. 确保 api-config.ets 已添加到 .gitignore 中
 * 
 * 获取API密钥：
 * 访问 https://www.dandanplay.com/ 注册账号并申请API密钥
 */

export interface DanDanPlayConfig {
  appId: string;
  appSecret: string;
  baseUrl: string;
}

export const danDanPlayConfig: DanDanPlayConfig = {
  // 在此填入你的弹弹play App ID
  appId: "YOUR_APP_ID_HERE",
  
  // 在此填入你的弹弹play App Secret
  appSecret: "YOUR_APP_SECRET_HERE",
  
  // 弹弹play API基础URL（通常不需要修改）
  baseUrl: "https://api.dandanplay.net/api/v2"
};